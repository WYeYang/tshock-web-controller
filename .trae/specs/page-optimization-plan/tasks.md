# TShock 控制器页面优化 - 实施计划

## [x] 任务 1: 删除不需要的页面文件

- **优先级**：P0
- **依赖**：None
- **描述**：
  - 删除 `NLPPage.tsx`
  - 删除 `HistoryPage.tsx`
  - 删除 `UserPage.tsx`
  - 删除这些页面中使用的但其他地方不需要的辅助函数或类型
- **验收标准**：AC-1
- **测试要求**：
  - `programmatic`：构建成功，无编译错误
  - `human-judgment`：检查导航栏不再有已删除页面的链接
- **备注**：检查 utils/storage 中是否有历史记录相关的函数可以删除

## [x] 任务 2: 创建新的配置面板组件 ConfigPanel

- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 基于现有 ConfigPage 创建新的 ConfigPanel 组件
  - 实现可折叠功能（useState 管理展开/收起状态）
  - 根据是否有配置决定默认展开/收起状态
  - 保持现有配置功能完整
- **验收标准**：AC-3, AC-4
- **测试要求**：
  - `human-judgment`：验证无配置时展开，有配置时收起
  - `human-judgment`：验证可以手动切换展开/收起
  - `programmatic`：验证保存/读取配置功能正常
- **备注**：组件应该可以被包含在其他页面中，不需要自己的完整布局

## [x] 任务 3: 创建新的主页面 Dashboard

- **优先级**：P0
- **依赖**：任务 2
- **描述**：
  - 创建 Dashboard.tsx 组件
  - 集成 ConfigPanel 在顶部
  - 集成 StatusPage 的功能（服务器状态、玩家管理）
  - 集成 CommandPage 的功能（命令执行）
  - 集成 HelpPage 的功能（帮助文档）
  - 使用选项卡或分段来组织不同功能
- **验收标准**：AC-2, AC-5
- **测试要求**：
  - `human-judgment`：验证所有功能都可以在新页面中访问
  - `programmatic`：验证各项功能正常工作
- **备注**：建议使用选项卡来组织内容，保持界面整洁

## [x] 任务 4: 更新 Navbar 组件

- **优先级**：P0
- **依赖**：任务 3
- **描述**：
  - 更新 Navbar.tsx 的 Page 类型
  - 删除不需要的导航项
  - 简化导航栏（可能只需要一个"控制台"导航项）
- **验收标准**：AC-1
- **测试要求**：
  - `human-judgment`：检查导航栏显示正确
- **备注**：保持良好的视觉设计

## [x] 任务 5: 更新 PageRouter 和 App.tsx

- **优先级**：P0
- **依赖**：任务 4
- **描述**：
  - 更新 PageRouter.tsx，移除对已删除页面的引用
  - 添加对新 Dashboard 页面的引用
  - 更新 App.tsx 中的默认页面
- **验收标准**：AC-1, AC-2
- **测试要求**：
  - `programmatic`：构建成功，路由正常工作
  - `human-judgment`：访问时显示新的 Dashboard 页面
- **备注**：

## [x] 任务 6: 清理和优化

- **优先级**：P1
- **依赖**：任务 5
- **描述**：
  - 删除不再需要的 imports
  - 检查是否有冗余代码可以清理
  - 运行构建确保无错误
- **验收标准**：AC-1, AC-5
- **测试要求**：
  - `programmatic`：构建成功，无警告
- **备注**：
